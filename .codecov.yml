coverage:
  status:
    project:
      default:
        target: 90
    patch:
      default:
        target: 90

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no